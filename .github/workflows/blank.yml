name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run very simple test
      run: |
        - bin/dfm install
        - source ~/.bashrc
